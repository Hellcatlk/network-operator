
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: switchports.metal3.io
spec:
  group: metal3.io
  names:
    kind: SwitchPort
    listKind: SwitchPortList
    plural: switchports
    singular: switchport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: state
      jsonPath: .status.state
      name: STATE
      type: string
    - description: error
      jsonPath: .status.error
      name: ERROR
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SwitchPort is the Schema for the switchports API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SwitchPortSpec defines the desired state of SwitchPort
            properties:
              configuration:
                description: The reference of PortConfiguration CR
                properties:
                  name:
                    type: string
                  namespace:
                    default: default
                    description: If empty use default namespace
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: SwitchPortStatus defines the observed state of SwitchPort
            properties:
              configuration:
                description: The current Configuration of the port
                properties:
                  apiVersion:
                    description: 'APIVersion defines the versioned schema of this
                      representation of an object. Servers should convert recognized
                      schemas to the latest internal value, and may reject unrecognized
                      values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                    type: string
                  kind:
                    description: 'Kind is a string value representing the REST resource
                      this object represents. Servers may infer this from the endpoint
                      the client submits requests to. Cannot be updated. In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: SwitchPortConfigurationSpec defines the desired state
                      of SwitchPortConfiguration
                    properties:
                      acls:
                        items:
                          description: ACL describes the rules applied in the switch
                          properties:
                            action:
                              enum:
                              - allow
                              - deny
                              type: string
                            des:
                              type: string
                            desPortRange:
                              pattern: ([0-9]{1,})|([0-9]{1,}-[0-9]{1,})(,([0-9]{1,})|([0-9]{1,}-[0-9]{1,}))*
                              type: string
                            protocol:
                              enum:
                              - TCP
                              - UDP
                              - ICMP
                              - ALL
                              type: string
                            src:
                              type: string
                            srcPortRange:
                              pattern: ([0-9]{1,})|([0-9]{1,}-[0-9]{1,})(,([0-9]{1,})|([0-9]{1,}-[0-9]{1,}))*
                              type: string
                            type:
                              enum:
                              - ipv4
                              - ipv6
                              type: string
                          type: object
                        maxItems: 10
                        type: array
                      disable:
                        description: Disable port
                        type: boolean
                      untaggedVLAN:
                        type: integer
                      vlans:
                        description: 'The range of tagged vlans. You can use `-` to
                          connect two numbers to express the range or use separate
                          numbers. You can use `,` to combine the above two methods,
                          for example: `1-10,11,13-20`'
                        pattern: ([0-9]{1,})|([0-9]{1,}-[0-9]{1,})(,([0-9]{1,})|([0-9]{1,}-[0-9]{1,}))*
                        type: string
                    type: object
                  status:
                    description: SwitchPortConfigurationStatus defines the observed
                      state of SwitchPortConfiguration
                    type: object
                type: object
              error:
                description: The error message of the port
                type: string
              state:
                description: The current configuration status of the port
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
